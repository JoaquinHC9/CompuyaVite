import Typography from '@mui/material/Typography';

const Products = () => (
  <div>
    <Typography variant="h4">Products</Typography>
    <Typography>Manage your products here.</Typography>
  </div>
);

export default Products;
