import Typography from '@mui/material/Typography';

const Categories = () => (
  <div>
    <Typography variant="h4">Categories</Typography>
    <Typography>Manage your categories here.</Typography>
  </div>
);

export default Categories;
