import Typography from '@mui/material/Typography';

const Customers = () => (
  <div>
    <Typography variant="h4">Customers</Typography>
    <Typography>Manage your customers here.</Typography>
  </div>
);

export default Customers;
